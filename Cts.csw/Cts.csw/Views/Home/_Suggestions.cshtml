@model SuggestionViewModel
@using Cts.csw.Models;

@*

    var result = @Html.Raw(Json.Encode(Intelliscents.ShowMethods(Model.CurrentWord)));*@

<script>
    function getHints(cm, options) {
        var cur = cm.getCursor();
        var result = @Html.Raw(Json.Encode(Model.Suggestions));
        var result2 = ['dog', 'cat']
        return {
            list: result,
            from: cur,
            to: cur
        };
    };
</script>
